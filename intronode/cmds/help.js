const help = `
    --mkdir [name] ............. Creates a directory with name
    --read  [path] ............. Reads a file in path
    --write [path] [message] ... Writes a file in path with message
`;

module.exports = console.log(help);